# SheetFilter

`SheetFilter` opens filter controls in a bottom sheet. Its close control is a circular icon-only button positioned just above the sheet rather than inside the sheet content.

The close control uses the shadcn `Button` and `SheetClose` primitives, is labelled for assistive technology, and closes the sheet without altering the selected filters.
